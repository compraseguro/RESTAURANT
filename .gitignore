# Dependencies
node_modules/
client/node_modules/

# Build output
client/dist/
dist/

# Runtime and local data
server/data/printing-config.json
uploads/
tmp_xlsx/
*.db
*.sqlite
*.sqlite3
*.db-shm
*.db-wal

# Environment files
.env
.env.*
!.env.example
!client/.env.example

# Logs and OS/editor cache
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Deployment/platform cache
.vercel/
.render/

# Cartas del menú (auto-pedido): no versionar; súbelas en el servidor o usa Admin → Subir.
client/public/cartas/*
!client/public/cartas/.gitkeep

# Raíz del repo (fotos sueltas de cartas)
IMG_20*.png

# Docker: credenciales locales (no subir)
docker/bot.env
docker/certs/

# E-fact (Python bajo server/efact): certificados y salida local (no subir)
server/efact/certs/
server/efact/output/

# Puente WhatsApp local (sesión + node_modules)
whatsapp-bridge/node_modules/
whatsapp-bridge/.wwebjs_auth/
whatsapp-bridge/.wwebjs_cache/
whatsapp-bridge/.env

# Local archives/backups
*.zip
*.7z
*.tar
*.tar.gz
